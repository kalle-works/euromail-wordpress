{
    "name": "kalle-works/euromail-wordpress",
    "description": "Euromail – SMTP & Email API WordPress plugin",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=7.4",
        "euromail/euromail-php": "@dev|^1.0"
    },
    "require-dev": {
        "yoast/phpunit-polyfills": "^1.1",
        "wp-coding-standards/wpcs": "^3.4",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.2"
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    },
    "scripts": {
        "lint": "phpcs",
        "phpcs": "phpcs",
        "phpcbf": "phpcbf"
    },
    "_todo_packagist": "euromail/euromail-php is not yet registered on Packagist (pending manual submission). Once it is, drop the 'repositories' entries below and require \"^1.0\" directly — every consumer of this package currently has to carry that vcs repository entry themselves, which a Packagist registration removes the need for.",
    "repositories": [
        {
            "type": "path",
            "url": "../euromail-php",
            "options": {
                "symlink": false
            }
        },
        {
            "type": "vcs",
            "url": "https://github.com/kalle-works/euromail-php"
        }
    ],
    "minimum-stability": "dev",
    "prefer-stable": true
}

# Euromail – SMTP & Email API

WordPress plugin that routes `wp_mail()` through the [euromail.dev](https://euromail.dev) transactional email API, with an SMTP fallback and a delivery log.

Development is in progress. See `CHANGELOG.md` once releases begin.
